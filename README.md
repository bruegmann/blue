# Blue monorepo

This monorepo contains the docs to Blue libraries and their packages.

## Development

You'll need `pnpm`. Run `pnpm install` to install all dependencies. Run `pnpm dev` to start development.
